# 你可以基于一个已有的样式，比如 LLVM, Google, Chromium, Microsoft, Mozilla
BasedOnStyle: LLVM

# --- 关键修改 ---

# 设置缩进宽度为4个字符
IndentWidth: 4

# 确保使用空格 (Never) 而不是 Tab (Always 或 ForIndentation)
UseTab: Never

# (可选) Tab键在编辑器中显示的宽度，通常设为与 IndentWidth 一致
TabWidth: 4